# Graph Algorithms

## Minimum Spanning Trees

A minimum spanning tree (MST) is a subset of edges in a connected, weighted, undirected graph that connects all vertices together with the minimum possible total edge weight and contains no cycles.

### Videos

- [Graph Algorithms](https://youtu.be/UJd3A0nrXg8) (7:59): Breadth- and depth-first traversals, spanning trees, representing graphs with a an adjacency matrix & adjacency lists.

## Shortest Paths

Shortest path algorithms find the path (or paths) between two vertices in a graph that has the smallest total sum of edge weights, with common variants handling single-source, all-pairs, or constrained cases (such as non-negative weights or the presence of negative edges).
